# Вайб — Ассистент с отзывами

Встречайте обновлённый сайт-визитку вашего ассистента «Вайб»! Теперь это полноценное веб-приложение с облачной базой данных, красивыми уведомлениями и удобным интерфейсом.

## 🌟 Основные возможности

- **Регистрация и вход** через email с подтверждением (настоящие письма от Firebase).
- **Сброс пароля** – письмо со ссылкой для восстановления.
- **Отзывы**: любой авторизованный пользователь может оставить отзыв с оценкой от 1 до 5 звёзд.
- **Все отзывы хранятся в облаке** (Firebase Firestore) и доступны каждому посетителю.
- **Три последних отзыва** выводятся на главной странице в стильных карточках со свечением.
- **Модальные окна** с плавной анимацией для входа, регистрации, написания отзыва и просмотра всех отзывов.
- **Кастомный выбор звёзд** – при наведении и выборе звёздочки меняют цвет.
- **Адаптивный дизайн** – отлично выглядит на мобильных устройствах.

## ✨ Новые фичи (последнее обновление)

1. **Внутрисайтовые уведомления**  
   Вместо стандартных `alert` теперь используются красивые всплывающие тосты в правом верхнем углу. Они различаются по цвету:
   - ✅ зелёные – успех (успешная регистрация, добавление отзыва);
   - 🔴 красные – ошибка (неверный пароль, проблемы с сетью);
   - 🔵 синие – информационные сообщения (выход из аккаунта).

2. **Поле отзыва больше нельзя растянуть**  
   Добавлено CSS-правило `resize: none` для textarea – теперь форма выглядит аккуратно и предсказуемо.

3. **Кнопка входа вместо часов**  
   В правом верхнем углу теперь отображается либо кнопка «Войти» (для неавторизованных), либо имя пользователя и кнопка выхода. Часы убраны – вместо них работает статус аутентификации.

4. **Сохранение сессии после перезагрузки**  
   Благодаря Firebase Authentication пользователь остаётся в системе даже после обновления страницы или повторного визита (если не нажал «Выход»).

5. **Облачное хранилище**  
   Все данные (пользователи, отзывы) теперь хранятся в Firebase Firestore, а не в `localStorage`. Это значит, что все посетители сайта видят одни и те же отзывы, а регистрация и вход работают по-настоящему.

6. **Улучшенный интерфейс модальных окон**  
   - Добавлены отступы между полями ввода (`margin-bottom`).
   - Кнопка закрытия выполнена в виде маленького кружка.
   - Добавлена кнопка «Забыли пароль?» прямо в форму входа.

## 📜 Что было раньше (история изменений)

- **Первая версия** – статичная страница с частицами на фоне, демонстрация возможностей ассистента (без отзывов).
- **Вторая версия** – добавлены отзывы, но данные хранились в `localStorage`. Каждый пользователь видел только свои отзывы. Использовались стандартные `alert` для сообщений.
- **Третья версия (текущая)** – полный переход на Firebase, добавлены уведомления, улучшен UI/UX.

## 🛠 Технологии

- **Firebase** (Authentication, Firestore, Hosting) – облачная платформа Google.
- **GitHub Pages** – хостинг статических файлов.
- **HTML5, CSS3, JavaScript** – чистый код без фреймворков.
- **Canvas API** – анимированные частицы на фоне.
- **Inter** – современный шрифт от Google Fonts.

## 🚀 Как развернуть у себя

1. Создайте три файла: `index.html`, `style.css`, `script.js`.
2. Скопируйте в них код из репозитория (или из чата).
3. Замените `firebaseConfig` в `script.js` на свои данные из консоли Firebase.
4. Залейте файлы на GitHub Pages или любой другой статический хостинг.
5. Готово! Все данные будут общими для всех пользователей.

## 📬 Обратная связь

Проект разработан специально для демонстрации возможностей Firebase и современного веб-дизайна. Если у вас есть идеи по улучшению или вы нашли баг – создайте issue в репозитории.

---

**DESIGNED BY REUZ0201 © 2026**  
*Возможно подстройка под ваш характер ;)*